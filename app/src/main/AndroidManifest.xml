<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scigh.activities">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.scigh.ScighTravel"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.scigh.activities.LauncherActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.scigh.activities.IntroductionActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.scigh.activities.HomeActivity"
            android:label="@string/app_name" />

    </application>

</manifest>
